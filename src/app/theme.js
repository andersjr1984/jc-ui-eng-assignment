import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#5e6572',
    },
    secondary: {
      main: '#7d98a1',
    },
    background: {
      default: '#eef1ef',
      paper: '#cc7e85',
    },
  },
});

export default theme;
