import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#5e6572',
    },
    secondary: {
      main: '#7d98a1',
    },
    background: {
      default: '#e8f0ff',
      paper: '#eef1ef',
    },
  },
});

export default theme;
